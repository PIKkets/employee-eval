<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evaluation.employee_eval.mapper.EvaluationElementMapper">
    
    <select id="findAll" resultType="EvaluationElement">
        SELECT * FROM EVALUATION_ELEMENT ORDER BY eval_type ASC, id ASC
    </select>
    
    <select id="findByType" resultType="EvaluationElement">
        SELECT * FROM EVALUATION_ELEMENT WHERE eval_type = #{evalType} ORDER BY id ASC
    </select>
    
    <select id="findById" resultType="EvaluationElement">
        SELECT * FROM EVALUATION_ELEMENT WHERE id = #{id}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO EVALUATION_ELEMENT (eval_type, name, weight) 
        VALUES (#{evalType}, #{name}, #{weight})
    </insert>
    
    <update id="update">
        UPDATE EVALUATION_ELEMENT 
        SET eval_type = #{evalType}, name = #{name}, weight = #{weight}
        WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM EVALUATION_ELEMENT WHERE id = #{id}
    </delete>

</mapper>
